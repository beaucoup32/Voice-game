const PrepWeekS2 = (props) => {
  const {scenario} = props;

  return (
    
    <p>{scenario ? scenario : "After taking a quick break you continue on but struggle trying to get one of your functions to work, what do you do?"}</p>
  );
};

export default PrepWeekS2;
